# Settings locais do Claude Code (não versionar).
.claude/settings.local.json

# Backlog local, não versionar.
BACKLOG.md

# Demandas de teste, dados sintéticos locais, não versionar.
demandas/

# Templates/stencils de referência com IP da empresa do usuário, nunca versionar.
templates-drawio/

# Venv Python local (POC drawpyo/graphviz), nunca versionar.
.venv/
__pycache__/
